//Declaramos a função que soma dois números recebidos como parâmetro
function somaDeDoisNumeros (numero1, numero2){
    //Declaramos a variavel soma e atribuimos o valor dela a soma dos dois números recebidos como parâmetro 
    let soma = numero1 + numero2;
    //retornamos a variavel soma
    return soma;
}